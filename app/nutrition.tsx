import { Alert, Pressable, StyleSheet, Text, View } from 'react-native';
import { Ionicons } from '@expo/vector-icons';
import { router } from 'expo-router';
import { AppScreen, MetricProgress, PrimaryButton } from '@/components';
import { colors, radius, spacing, typography } from '@/constants';
import { useDemoStore } from '@/features/demo';
import { calculateNutrition, nutritionGoals, weeklyCalories } from '@/features/nutrition/nutrition';
import type { MealType } from '@/types';

const meals: MealType[] = ['Breakfast', 'Lunch', 'Dinner', 'Snacks'];
export default function NutritionScreen() {
  const { foodEntries, removeFood, waterGlasses, setWaterGlasses } = useDemoStore(); const totals = calculateNutrition(foodEntries); const maxWeek = Math.max(...weeklyCalories, nutritionGoals.calories); const average = Math.round(weeklyCalories.filter(Boolean).reduce((sum, value) => sum + value, 0) / weeklyCalories.filter(Boolean).length);
  return <AppScreen><View style={styles.header}><Pressable accessibilityLabel="Go back" onPress={() => router.back()}><Ionicons name="arrow-back" size={24} color={colors.text} /></Pressable><Text style={styles.headerTitle}>Today’s nutrition</Text><Pressable accessibilityLabel="Nutrition history" onPress={() => Alert.alert('Nutrition history', 'The chart below is the no-cost demo history. Cloud history connects after funding.')}><Ionicons name="calendar-outline" size={23} color={colors.text} /></Pressable></View>
    <View style={styles.calorieCard}><View><Text style={styles.eyebrow}>CALORIES</Text><Text style={styles.calories}>{totals.calories.toLocaleString()}</Text><Text style={styles.remaining}>{Math.max(0, nutritionGoals.calories - totals.calories).toLocaleString()} remaining</Text></View><View style={styles.ring}><Text style={styles.ringValue}>{Math.round(totals.calories / nutritionGoals.calories * 100)}%</Text></View></View>
    <View style={styles.macros}><MetricProgress label="Protein" value={totals.protein} goal={nutritionGoals.protein} unit="g" color="#86EFAC" /><MetricProgress label="Carbohydrates" value={totals.carbs} goal={nutritionGoals.carbs} unit="g" color="#A5F3FC" /><MetricProgress label="Fat" value={totals.fat} goal={nutritionGoals.fat} unit="g" color="#FDBA74" /></View>
    <View style={styles.water}><View><Text style={styles.sectionTitle}>WATER</Text><Text style={styles.waterText}>{waterGlasses} of {nutritionGoals.water} glasses</Text></View><View style={styles.waterActions}><Pressable accessibilityLabel="Remove water" onPress={() => setWaterGlasses(waterGlasses - 1)} style={styles.waterButton}><Ionicons name="remove" size={20} color={colors.text} /></Pressable><Ionicons name="water" size={25} color="#67E8F9" /><Pressable accessibilityLabel="Add water" onPress={() => setWaterGlasses(waterGlasses + 1)} style={styles.waterButton}><Ionicons name="add" size={20} color={colors.background} /></Pressable></View></View>
    <View style={styles.weekCard}><View style={styles.weekTop}><View><Text style={styles.sectionTitle}>7-DAY CALORIE TREND</Text><Text style={styles.average}>{average.toLocaleString()} avg</Text></View><Text style={styles.onTrack}>ON TRACK</Text></View><View style={styles.chart}>{weeklyCalories.map((value, index) => <View key={index} style={styles.barColumn}><View style={[styles.bar, { height: Math.max(5, value / maxWeek * 80), backgroundColor: value ? (value > nutritionGoals.calories ? '#FDBA74' : colors.accent) : colors.border }]} /><Text style={styles.day}>{['M','T','W','T','F','S','S'][index]}</Text></View>)}</View></View>
    <View style={styles.mealHeader}><Text style={styles.title}>Meals</Text><Pressable onPress={() => router.push('/(tabs)/scanner')}><Text style={styles.add}>+ Add food</Text></Pressable></View>
    {meals.map((meal) => { const entries = foodEntries.filter((entry) => entry.meal === meal); const calories = entries.reduce((sum, entry) => sum + entry.calories, 0); return <View key={meal} style={styles.meal}><View style={styles.mealTop}><Text style={styles.mealName}>{meal}</Text><Text style={styles.mealCalories}>{calories} kcal</Text></View>{entries.length ? entries.map((entry) => <Pressable key={entry.id} onLongPress={() => Alert.alert('Remove entry?', entry.name, [{ text: 'Cancel', style: 'cancel' }, { text: 'Remove', style: 'destructive', onPress: () => removeFood(entry.id) }])} style={styles.entry}><View><Text style={styles.entryName}>{entry.name}</Text><Text style={styles.entryMacro}>P {entry.protein}g · C {entry.carbs}g · F {entry.fat}g</Text></View><Text style={styles.entryCalories}>{entry.calories}</Text></Pressable>) : <Pressable onPress={() => router.push({ pathname: '/(tabs)/scanner', params: { meal } })} style={styles.emptyMeal}><Text style={styles.emptyMealText}>Add {meal.toLowerCase()}</Text><Ionicons name="add-circle-outline" size={19} color={colors.textMuted} /></Pressable>}</View>; })}
    <View style={styles.bottom}><PrimaryButton label="Scan or search food" onPress={() => router.push('/(tabs)/scanner')} /></View>
  </AppScreen>;
}
const styles = StyleSheet.create({ header: { flexDirection: 'row', alignItems: 'center', justifyContent: 'space-between', marginBottom: spacing.xl }, headerTitle: { ...typography.heading, color: colors.text }, calorieCard: { flexDirection: 'row', justifyContent: 'space-between', alignItems: 'center', padding: spacing.lg, borderRadius: radius.lg, backgroundColor: '#242419', borderWidth: 1, borderColor: 'rgba(245,217,10,.25)' }, eyebrow: { ...typography.label, color: colors.accent }, calories: { color: colors.text, fontSize: 38, fontWeight: '900', letterSpacing: -1, marginTop: 3 }, remaining: { color: colors.textMuted, fontSize: 12 }, ring: { width: 82, height: 82, borderRadius: 41, borderWidth: 8, borderColor: colors.accent, alignItems: 'center', justifyContent: 'center' }, ringValue: { color: colors.text, fontWeight: '900' }, macros: { gap: spacing.md, marginTop: spacing.lg }, water: { flexDirection: 'row', alignItems: 'center', justifyContent: 'space-between', padding: spacing.md, borderRadius: radius.md, backgroundColor: colors.glass, marginTop: spacing.lg }, sectionTitle: { ...typography.label, color: colors.textMuted, fontSize: 9 }, waterText: { color: colors.text, fontWeight: '800', marginTop: 4 }, waterActions: { flexDirection: 'row', alignItems: 'center', gap: spacing.sm }, waterButton: { width: 34, height: 34, borderRadius: 17, backgroundColor: colors.accent, alignItems: 'center', justifyContent: 'center' }, weekCard: { padding: spacing.md, borderRadius: radius.md, backgroundColor: colors.glass, marginTop: spacing.lg }, weekTop: { flexDirection: 'row', justifyContent: 'space-between' }, average: { color: colors.text, fontWeight: '900', fontSize: 20, marginTop: 3 }, onTrack: { ...typography.label, color: colors.success, fontSize: 8 }, chart: { height: 110, flexDirection: 'row', alignItems: 'flex-end', justifyContent: 'space-around', marginTop: spacing.md }, barColumn: { alignItems: 'center', justifyContent: 'flex-end', height: '100%', gap: 5 }, bar: { width: 20, borderRadius: 6 }, day: { color: colors.textMuted, fontSize: 9 }, mealHeader: { flexDirection: 'row', alignItems: 'center', justifyContent: 'space-between', marginTop: spacing.xl, marginBottom: spacing.md }, title: { ...typography.heading, color: colors.text }, add: { color: colors.accent, fontWeight: '800' }, meal: { padding: spacing.md, borderRadius: radius.md, backgroundColor: colors.surfaceElevated, marginBottom: spacing.sm }, mealTop: { flexDirection: 'row', justifyContent: 'space-between' }, mealName: { color: colors.text, fontWeight: '900' }, mealCalories: { color: colors.textMuted, fontSize: 12 }, entry: { flexDirection: 'row', justifyContent: 'space-between', alignItems: 'center', paddingTop: spacing.md, marginTop: spacing.sm, borderTopWidth: 1, borderTopColor: colors.border }, entryName: { color: colors.text, fontWeight: '700' }, entryMacro: { color: colors.textMuted, fontSize: 10, marginTop: 3 }, entryCalories: { color: colors.accent, fontWeight: '900' }, emptyMeal: { flexDirection: 'row', justifyContent: 'space-between', paddingTop: spacing.md }, emptyMealText: { color: colors.textMuted }, bottom: { marginTop: spacing.lg } });
