import { StyleSheet, Text, View } from 'react-native';
import { colors, spacing, typography } from '@/constants';
export function VideoCaption({ caption, hashtags, categories }: { caption: string; hashtags: string[]; categories: string[] }) { return <View><Text numberOfLines={3} style={styles.caption}>{caption}</Text><Text numberOfLines={2} style={styles.hashtags}>{hashtags.map((tag) => `#${tag}`).join('  ')}</Text><View style={styles.categories}>{categories.map((category) => <View key={category} style={styles.category}><Text style={styles.categoryText}>{category.toUpperCase()}</Text></View>)}</View></View>; }
const styles = StyleSheet.create({ caption: { ...typography.body, color: colors.text, marginTop: spacing.sm, maxWidth: 320, textShadowColor: 'rgba(0,0,0,.6)', textShadowRadius: 4 }, hashtags: { color: colors.accentSoft, fontSize: 13, fontWeight: '800', marginTop: 5 }, categories: { flexDirection: 'row', gap: spacing.sm, marginTop: spacing.sm }, category: { borderWidth: 1, borderColor: 'rgba(255,255,255,.22)', backgroundColor: 'rgba(0,0,0,.25)', paddingHorizontal: 8, paddingVertical: 4, borderRadius: 999 }, categoryText: { color: colors.text, fontSize: 8, fontWeight: '900', letterSpacing: .8 } });
