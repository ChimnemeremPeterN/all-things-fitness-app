import { StyleSheet, Text, View } from 'react-native';
import { colors, radius, spacing } from '@/constants';
export function MetricProgress({ label, value, goal, unit, color = colors.accent }: { label: string; value: number; goal: number; unit: string; color?: string }) { const progress = Math.min(1, value / Math.max(goal, 1)); return <View style={styles.wrap}><View style={styles.top}><Text style={styles.label}>{label}</Text><Text style={styles.value}>{Math.round(value)}<Text style={styles.goal}> / {goal}{unit}</Text></Text></View><View style={styles.track}><View style={[styles.fill, { backgroundColor: color, width: `${progress * 100}%` }]} /></View></View>; }
const styles = StyleSheet.create({ wrap: { gap: spacing.sm }, top: { flexDirection: 'row', justifyContent: 'space-between', alignItems: 'center' }, label: { color: colors.text, fontWeight: '700', fontSize: 12 }, value: { color: colors.text, fontWeight: '900', fontSize: 12 }, goal: { color: colors.textMuted, fontWeight: '500' }, track: { height: 7, borderRadius: radius.pill, backgroundColor: colors.border, overflow: 'hidden' }, fill: { height: '100%', borderRadius: radius.pill } });
